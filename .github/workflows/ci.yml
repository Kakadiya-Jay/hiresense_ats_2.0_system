name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12.1
      - name: Install deps
        run: |
          python -m venv .hirevenv
          source .hirevenv/Scripts/activate
          pip install -r requirements.txt
          Deactivate
      - name: Run tests
        run: pytest -q
